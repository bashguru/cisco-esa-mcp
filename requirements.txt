# Runtime dependencies. In Docker, torch (CPU build) is installed first from the
# PyTorch CPU index (see Dockerfile), so it is intentionally NOT listed here.
# For a bare pip install without Docker:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

fastmcp>=2.10
docling>=2.15
sentence-transformers>=3.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
pgvector>=0.3.6
PyJWT[crypto]>=2.8
httpx>=0.27
PyYAML>=6.0
uvicorn[standard]>=0.30
numpy>=1.26
tqdm>=4.66
python-dotenv>=1.0
